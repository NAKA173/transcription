# Audio to MIDI 完全マニュアル

**「何もわからない人」のための、ゼロからの手順書**

---

## 目次

1. [必要なもの](#1-必要なもの)
2. [Pythonのインストール](#2-pythonのインストール)
3. [このアプリをダウンロード](#3-このアプリをダウンロード)
4. [アプリを起動する](#4-アプリを起動する)
5. [使い方](#5-使い方)
6. [困ったとき](#6-困ったとき)
7. [用語集](#7-用語集)

---

## 1. 必要なもの

| 項目 | 詳細 |
|------|------|
| パソコン | Mac / Windows / Linux どれでもOK |
| インターネット | 初回のみ必要（ダウンロード用） |
| 変換したい音声ファイル | MP3, WAV, OGG, FLAC, M4A などに対応 |

**スマホでは動きません。パソコンが必要です。**

---

## 2. Pythonのインストール

このアプリは「Python」というプログラム言語で動いています。
まず、お使いのパソコンにPythonが入っているか確認しましょう。

### Mac の場合

1. **ターミナルを開く**
   - `Cmd + スペース` を押す（Spotlight検索が開く）
   - 「ターミナル」と入力して `Enter`
   - 黒い画面が開きます → これが「ターミナル」です

2. **Python があるか確認する**
   - ターミナルに以下をコピペして `Enter`:
   ```
   python3 --version
   ```
   - `Python 3.11.x` のように表示されたら → **もう入っています！ → [3. へ進む](#3-このアプリをダウンロード)**
   - `command not found` と出たら → インストールが必要

3. **Python をインストール**
   - ブラウザで https://www.python.org/downloads/ を開く
   - 黄色い「Download Python 3.x.x」ボタンをクリック
   - ダウンロードしたファイル (.pkg) をダブルクリック
   - 「続ける」→「続ける」→「インストール」をクリック
   - パスワードを入力して完了
   - **ターミナルを一度閉じて、もう一度開く**
   - もう一度 `python3 --version` で確認

### Windows の場合

1. **コマンドプロンプトを開く**
   - `Windows キー` を押す
   - 「cmd」と入力して `Enter`
   - 黒い画面が開きます → これが「コマンドプロンプト」です

2. **Python があるか確認する**
   - 以下をコピペして `Enter`:
   ```
   python --version
   ```
   - `Python 3.11.x` と表示 → **もう入っています！ → [3. へ進む](#3-このアプリをダウンロード)**
   - エラーが出たら → インストールが必要

3. **Python をインストール**
   - ブラウザで https://www.python.org/downloads/ を開く
   - 黄色い「Download Python 3.x.x」ボタンをクリック
   - ダウンロードした .exe をダブルクリック
   - **⚠️ 重要: 「Add python.exe to PATH」にチェックを入れる ⚠️**
   - 「Install Now」をクリック
   - **コマンドプロンプトを一度閉じて、もう一度開く**
   - もう一度 `python --version` で確認

---

## 3. このアプリをダウンロード

### 方法A: ZIPでダウンロード（一番簡単）

1. ブラウザで以下を開く:
   https://github.com/naka173/transcription
2. 緑色の「Code」ボタンをクリック
3. 「Download ZIP」をクリック
4. ダウンロードされた ZIP を展開（ダブルクリック）
5. 展開されたフォルダ `transcription-main` を覚えておく

### 方法B: gitでダウンロード（推奨）

「git」がインストール済みの場合はこちらの方が便利です。

1. ターミナル（Mac）/ コマンドプロンプト（Windows）を開く
2. 以下をコピペして `Enter`:
   ```
   git clone https://github.com/naka173/transcription.git
   ```
3. `transcription` フォルダが作成されます

**git が入ってない場合:**
- Mac: ターミナルで `git` と入力 → インストールを促されるので「インストール」をクリック
- Windows: https://git-scm.com/download/win からダウンロード＆インストール

---

## 4. アプリを起動する

### Mac の場合

1. **ターミナルを開く**（[上の手順](#mac-の場合) 参照）

2. **アプリのフォルダに移動する**
   - ZIPの場合:
   ```
   cd ~/Downloads/transcription-main
   ```
   - gitの場合:
   ```
   cd transcription
   ```
   ※もしフォルダの場所がわからない場合、Finderでフォルダを見つけて、
   ターミナルに `cd ` (cdの後にスペース) と入力してから、フォルダをターミナルにドラッグ＆ドロップしてEnter

3. **起動！**
   ```
   bash start.sh
   ```

4. こんな表示が出たら成功：
   ```
   -----------------------------------
     http://localhost:8000
     ブラウザで上のURLを開いてください
     停止: Ctrl+C
   -----------------------------------
   ```

5. **ブラウザで http://localhost:8000 を開く**

### Windows の場合

1. エクスプローラーで `transcription` フォルダを開く
2. `start.bat` をダブルクリック
3. 黒い画面が開いて、パッケージのインストールが始まる
4. `http://localhost:8000` と表示されたらブラウザで開く

**または、コマンドプロンプトから：**
```
cd Downloads\transcription-main
start.bat
```

---

## 5. 使い方

### 基本の変換

1. ブラウザで http://localhost:8000 を開く
2. 🎵 の部分をクリックして音声ファイルを選択（またはドラッグ＆ドロップ）
3. 「MIDIに変換」ボタンをクリック
4. 変換完了後、「MIDIファイルをダウンロード」をクリック

### タブの説明

| タブ | 機能 |
|------|------|
| **変換** | メインの変換画面。1ファイルずつ変換 |
| **一括変換** | 複数ファイルをまとめて変換（最大20ファイル） |
| **精度比較** | 正解MIDIと変換結果を比較して精度を測定 |
| **履歴** | 過去の変換結果をダウンロードし直せる |

### 設定の調整

「詳細設定」をクリックすると細かい設定ができます:

| 設定 | 意味 | おすすめ |
|------|------|---------|
| ノート検出感度 | 低くすると多くの音を拾う | 0.50 - 0.70 |
| フレーム確信度 | 音の長さの判定 | 0.40 - 0.60 |
| 最短ノート長 | 短すぎるノートを除去 | 100 - 300ms |
| 最小ベロシティ | 弱い音を除去 | 20 - 40 |
| ノーマライズ＆ノイズ除去 | 音質改善（ONがおすすめ） | ON |
| 楽器を分離 | ドラム・ベース等を別トラックに | お好みで |
| ドラムを詳細分類 | Kick/Snare/Hi-Hat等に分離 | 楽器分離ON時 |
| クォンタイズ | リズムを正確にする | ON |

### プリセット

| プリセット | 用途 |
|-----------|------|
| クリーン | ノイズが少ない綺麗な音源 |
| バランス | 一般的な音源（デフォルト） |
| 高感度 | 小さい音や弱い音も拾いたい時 |

### MIDI プレビュー

変換後に表示される ▶ ボタンでMIDIをブラウザ上で試聴できます。

### 楽譜表示

変換後に「楽譜を表示」ボタンで簡易楽譜を表示できます。

### ドラム詳細分類

「楽器を分離」をONにしてから「ドラムを詳細分類」をONにすると、
ドラムが以下のパートに分かれます:
- Kick（バスドラム）
- Snare（スネア）
- Hi-Hat（ハイハット）
- Tom（タム）
- Crash（クラッシュシンバル）
- Ride（ライド）

---

## 6. 困ったとき

### Q: `bash: start.sh: No such file or directory` と出る

**A:** アプリのフォルダに移動できていません。
```
# 今いる場所を確認
pwd

# 正しいフォルダに移動（例）
cd ~/Downloads/transcription-main
ls start.sh   # ← ファイルがあるか確認
```

### Q: `python3: command not found` と出る

**A:** Pythonがインストールされていません。[2. の手順](#2-pythonのインストール) へ。

### Q: ブラウザで開いてもエラーが出る

**A:** ターミナルに戻って、エラーが出ていないか確認してください。
よくある原因:
- ポート8000が使用中 → `bash start.sh 8001` で別ポートを使う
- パッケージのインストール失敗 → `pip3 install fastapi uvicorn basic-pitch pretty-midi soundfile scipy` を手動実行

### Q: 「ERR_ALPN_NEGOTIATION_FAILED」と出る

**A:** start.sh を使って起動してください。直接 uvicorn を起動する場合は `--http h11` オプションが必要です。

### Q: OGG/M4A ファイルが変換できない

**A:** ffmpeg のインストールが必要です:
- Mac: `brew install ffmpeg`（Homebrewが必要）
- Windows: https://ffmpeg.org/download.html
- Ubuntu: `sudo apt install ffmpeg`

### Q: 「楽器分離」でエラーが出る

**A:** Demucs (大きいパッケージ) の追加インストールが必要です:
```
pip3 install demucs torchaudio
```
※ 数GBのダウンロードになります

### Q: アプリを止めたい

**A:** ターミナルで `Ctrl + C` を押す

### Q: 2回目以降の起動

**A:** 毎回同じ手順です:
```
cd transcription        # ← フォルダに移動
bash start.sh           # ← 起動
# ブラウザで http://localhost:8000 を開く
```

---

## 7. 用語集

| 用語 | 意味 |
|------|------|
| **ターミナル** | コマンド（文字の命令）を入力する画面。Macでは「ターミナル」、Windowsでは「コマンドプロンプト」 |
| **コマンド** | ターミナルに入力する文字の命令。`cd` は移動、`ls` はファイル一覧 |
| **ディレクトリ / フォルダ** | ファイルが入っている場所。同じ意味 |
| **cd** | 「change directory」の略。フォルダを移動するコマンド |
| **MIDI** | 音楽データの形式。音の高さ・長さ・強さの情報が入っている。DAWで編集可能 |
| **DAW** | 音楽制作ソフト（GarageBand, Logic Pro, FL Studio, Cubase など） |
| **pip** | Pythonのパッケージ管理ツール。追加機能をインストールする時に使う |
| **git** | コードのバージョン管理ツール。ダウンロードにも使える |
| **GitHub** | gitで管理されたコードを共有するウェブサイト |
| **localhost** | 自分のパソコン自身を指すアドレス |
| **ポート** | パソコン内のサービスの「窓口番号」。このアプリは8000番を使う |
| **BPM** | Beats Per Minute。1分間の拍数。曲の速さを表す |
| **クォンタイズ** | 音符のタイミングを正確なリズムに揃える処理 |
| **ffmpeg** | 音声・動画を変換する無料ツール。OGG/M4A等の変換に必要 |
| **Demucs** | Meta社が開発した、音楽を楽器ごとに分離するAI |

---

## クイックリファレンス（早見表）

```
# ===== 毎回やること =====

# 1. ターミナルを開く
# 2. アプリのフォルダに移動
cd transcription

# 3. 起動
bash start.sh

# 4. ブラウザで開く
#    http://localhost:8000

# 5. 止める時は Ctrl+C
```
